import './globals.css';

export const metadata = { title: 'Cifras PDF', description: 'Selecione trechos de cifras e gere um PDF para tocar.' };

export default function RootLayout({ children }) {
  return <html lang="pt-BR"><body>{children}</body></html>;
}
